{
  "name": "smartfeed-ai-mern",
  "private": true,
  "scripts": {
    "dev": "node scripts/free-ports.mjs && node scripts/dev.mjs",
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
