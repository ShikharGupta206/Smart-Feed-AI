{
  "name": "smartfeed-ai-mern",
  "private": true,
  "scripts": {
    "dev": "node scripts/free-ports.mjs && concurrently -k \"npm --prefix backend start\" \"npm --prefix frontend run dev -- --host 127.0.0.1\"",
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
